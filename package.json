{
  "name": "vardha-group-ecosystem",
  "version": "1.0.0",
  "description": "Vardha Group Ecosystem Platform",
  "scripts": {
    "install-all": "npm install && npm install --prefix client",
    "dev": "npm run dev --prefix client"
  }
}
